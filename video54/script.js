console.log("hi");
// var a=prompt("enter a number");
// var b=confirm("are you you wnat to blast your laptop?");
if(b){
    console.log("blasting");
}
else{
    console.log("not blasting!!");
}
//node js doesnot run prompt ,alert and confirm
// Because Node.js does NOT have a browser window.
// 👉 And alert, prompt, confirm need a browser window to show pop-ups.
// 🔹 Node.js = Backend (no browser, no screen)

// Node runs in your terminal, not in a browser.

// Node.js has NO window, so there is no place to show a popup.

// So when Node sees:
// **alert, prompt, confirm belong to the browser,

// and Node.js is NOT a browser.**